# ============================================
# TELEBOT PANEL PRO - ENVIRONMENT CONFIGURATION
# ============================================

# JWT & SECURITY CONFIGURATION
JWT_SECRET=f8d7a3c6b5e49201738a1d2f4c9b6a7e5d3c2b1a0f9e8d7c6b5a49382716f5e4d3c2
TOKEN_EXPIRY=28800000  # 8 hours in milliseconds
SALT_ROUNDS=10

# ADMIN CREDENTIALS
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2a$10$XcVvjL7T8q5NwRzYm9KjE.A6b8cDfG.HjKlMnOpQrStUvWxYz1234

# SECURITY SETTINGS
IP_WHITELIST=127.0.0.1,::1,49.156.45.218
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8888,https://*.netlify.app
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=900000  # 15 minutes
MAX_BOTS_PER_USER=10
MAX_USER_BOTS=5
MAX_SESSION_AGE=86400000  # 24 hours

# TELEGRAM CONFIGURATION
TELEGRAM_API_ID=  # Get from https://my.telegram.org
TELEGRAM_API_HASH=  # Get from https://my.telegram.org

# AUTO-TEXT CONFIGURATION
AUTO_TEXT_MAX_TARGETS=50
MIN_INTERVAL_SECONDS=10
MAX_MESSAGE_HISTORY=1000

# TERMINAL SECURITY
ALLOWED_COMMANDS=pwd,whoami,date,uptime,uname -a,hostname,ls,ls -la,ls -lh,cat package.json,cat README.md,ps aux | head -20,ps aux | grep bot,node --version,npm --version,python --version,curl -s https://api.ipify.org,ping -c 1 8.8.8.8,free -h,df -h,tail -n 50,head -n 10,wc -l,grep -i "error",echo,date +"%Y-%m-%d",clear,help,history,status,session,bots,userbots,ip

# LOGGING
LOG_LEVEL=info
LOG_RETENTION_DAYS=7

# PERFORMANCE
REQUEST_TIMEOUT=30000
MAX_REQUEST_SIZE=1048576  # 1MB
CONCURRENT_REQUESTS=100
