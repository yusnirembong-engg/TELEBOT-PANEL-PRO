{
  "name": "telebot-panel-pro",
  "version": "2.0.0",
  "description": "Advanced Telegram Bot Control Panel with User Bot Management, Secure Terminal, and Real-time Monitoring",
  "main": "index.js",
  "scripts": {
    "dev": "serve public -p 8888 -s",
    "build": "echo 'Building TeleBot Panel Pro...' && cp -r netlify/functions/package.json netlify/functions/node_modules/ ../ || true",
    "start": "npm run dev",
    "deploy": "bash deploy.sh",
    "setup": "bash deploy.sh --setup",
    "test": "echo 'Running tests...' && node -e \"console.log('✓ All systems ready')\"",
    "generate:hash": "node -e \"const bcrypt = require('bcryptjs'); const hash = bcrypt.hashSync(process.argv[2] || 'TeleBotPro@2024!', 10); console.log('Password hash:', hash)\"",
    "generate:secret": "node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\"",
    "generate:env": "cp .env.example .env && echo '.env file created. Please edit with your values.'",
    "lint": "echo 'Linting...'",
    "clean": "rm -rf node_modules netlify/functions/node_modules",
    "postinstall": "cd netlify/functions && npm install",
    "predeploy": "npm run build"
  },
  "keywords": [
    "telegram",
    "bot",
    "panel",
    "userbot",
    "automation",
    "control-panel",
    "dashboard",
    "telegram-bot",
    "telegram-api",
    "netlify",
    "serverless"
  ],
  "author": "TeleBot Pro Team",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "axios": "^1.6.0",
    "crypto-js": "^4.2.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/telebot-panel-pro.git"
  },
  "bugs": {
    "url": "https://github.com/yourusername/telebot-panel-pro/issues"
  },
  "homepage": "https://telebot-panel-pro.netlify.app",
  "private": true
}
